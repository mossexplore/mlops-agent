# Optional dependencies. Without them the retriever runs in pure-Python TF-IDF
# mode. Installing them enables hybrid retrieval (sparse TF-IDF + dense
# embeddings via sentence-transformers + faiss, fused with RRF); see
# scripts/embedding.py and scripts/retriever.py. Heavy (pulls torch).
langchain-core>=0.3.0
langgraph>=0.2.0
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
markdown-it-py>=3.0.0
numpy>=1.26.0
